# Required
ANTHROPIC_API_KEY=sk-ant-...

# Optional — only needed for actually sending emails (otherwise prints to stdout)
RESEND_API_KEY=re_...
AMANE_EMAIL=amane@example.com

# Phase 2 — Gmail API for LinkedIn email parsing
# GMAIL_CREDENTIALS_JSON=...
# GMAIL_TOKEN_JSON=...
